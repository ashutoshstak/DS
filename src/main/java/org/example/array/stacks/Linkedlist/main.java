package org.example.array.stacks.Linkedlist;

public class main {
    public static void main(String[] args) {

        // let us push and pop the elenent in the stack implemenated via linked list

        Linkedlistimp linkedlistimp = new Linkedlistimp();

        linkedlistimp.push("a");
        linkedlistimp.push("b");
        linkedlistimp.push("c");
        linkedlistimp.push("d");
        linkedlistimp.push("e");
        linkedlistimp.push("f");
        linkedlistimp.pop();
        linkedlistimp.pop();
        linkedlistimp.pop();
        linkedlistimp.pop();
        linkedlistimp.pop();
        linkedlistimp.pop();

    }
}
